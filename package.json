{
  "name": "telos",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "web:dev": "pnpm --filter ./apps/web dev",
    "registry:run": "make -C apps/registry run",
    "api-gateway:run": "make -C apps/api-gateway run",
    "auth-service:run": "make -C services/auth-service run"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "husky": "^9.1.7"
  }
}