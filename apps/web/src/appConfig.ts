const appConfig = {
  appName: 'Telos',
  baseDomain: process.env.NEXT_PUBLIC_DOMAIN!,
  locales: ['en', 'zh'],
  defaultLocale: 'en',
  colorScheme: 'light',
  subjectColor: '#6c35de',
}

export default appConfig
