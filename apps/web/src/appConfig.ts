const appConfig = {
  appName: 'Vidfly',
  baseDomain: process.env.NEXT_PUBLIC_DOMAIN!,
  locales: ['en', 'zh'],
  defaultLocale: 'en',
  colorScheme: 'light',
  subjectColor: '#7e15f4',
}

export default appConfig
